#input()  - this is input function
#always remember - by default the value of input is always in String(Str) type
"""
so we need to convert the type of data according to our use case
For example: 
-> age_int = int(age)

then it will convert to integer
 """

age = input('Enter your age: ')
print(f"You are {age} years old")